import { LoginPageProps } from 'uibee/components';
export default function LoginPage({ title, description, redirectPath, version, btg, handleSubmit, guestRedirectPath, guestText }: LoginPageProps): import("react").JSX.Element;
